version: 2
updates:
  # Keep the SHA-pinned GitHub Actions bumped with a review gate, so the pins in
  # ci.yml / release.yml don't silently rot.
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
