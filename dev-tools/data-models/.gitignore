# Regenerable raw sample records — recreate with sample_collections.py (needs an
# API key). The committed baseline of what Vulners serves is the generated
# src/vulners/_models/_collections_data.py, so nothing here needs committing.
type_schemas.json
collection_map.json
